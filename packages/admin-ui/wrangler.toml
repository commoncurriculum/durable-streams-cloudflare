name = "durable-stream-admin"
main = "src/worker.ts"
compatibility_date = "2024-01-01"

[vars]
CORE_URL = "http://localhost:8787"

[[d1_databases]]
binding = "ADMIN_DB"
database_name = "admin"
database_id = "placeholder-admin-db-id"

# Service binding to core worker (recommended for production)
# Provides lower latency and no auth needed between services
# [[services]]
# binding = "CORE"
# service = "durable-streams"

# Cloudflare API credentials for Analytics Engine
# CF_ACCOUNT_ID = ""
# METRICS_API_TOKEN = ""
