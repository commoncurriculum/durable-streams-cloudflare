{
  "name": "durable-streams-root",
  "private": true,
  "scripts": {
    "build": "pnpm -C packages/durable-stream-server run build",
    "test": "pnpm -C packages/durable-stream-server run test",
    "lint": "pnpm -C packages/durable-stream-server run lint",
    "typecheck": "pnpm -C packages/durable-stream-server run typecheck",
    "format": "pnpm -C packages/durable-stream-server run format",
    "format:check": "pnpm -C packages/durable-stream-server run format:check"
  }
}
