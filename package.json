{
  "name": "durable-streams-root",
  "private": true,
  "packageManager": "pnpm@10.29.2",
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "pnpm -r run clean",
    "format": "pnpm -r run format",
    "format:check": "pnpm -r run format:check",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "test:all": "pnpm -r run typecheck && pnpm -r run format:check && pnpm -r run lint && pnpm -C packages/core run test:unit && pnpm -C packages/server run test:unit && pnpm -C packages/core run conformance && pnpm -C packages/server run conformance && pnpm -r --filter '!@durable-streams-cloudflare/admin-core' --filter '!@durable-streams-cloudflare/admin-subscription' run test && pnpm -C packages/admin-core run test && pnpm -C packages/admin-subscription run test && pnpm -C packages/subscription run test:integration",
    "typecheck": "pnpm -r run typecheck",
    "dev": "pnpm --filter @durable-streams-cloudflare/core --filter @durable-streams-cloudflare/subscription --filter @durable-streams-cloudflare/admin-core --filter @durable-streams-cloudflare/admin-subscription --parallel dev"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.2",
    "@changesets/cli": "^2.29.8",
    "oxfmt": "^0.28.0",
    "oxlint": "^1.43.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ],
    "overrides": {
      "vitest": "4.1.0-beta.1",
      "@vitest/runner": "4.1.0-beta.1",
      "@vitest/snapshot": "4.1.0-beta.1"
    }
  }
}
